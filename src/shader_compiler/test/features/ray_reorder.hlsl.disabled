RWStructuredBuffer<uint> outp;

[[oxc::extension("RayReorder")]]
[[oxc::model("6.9")]]
[shader("raygeneration")]
void main() {
	dx::HitObject hit = dx::HitObject::MakeNop();
	dx::MaybeReorderThread(hit);
	outp[DispatchRaysIndex().x] = hit.IsNop() ? 1u : 0u;
}
