<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="{APP_PACKAGE}"
          android:versionCode="1"
          android:versionName="{APP_VERSION}">

  <uses-sdk android:minSdkVersion="{APP_API_LEVEL}" />

  <!-- Vulkan 1.1 + astc, sample rate shading, a few others -->

  <uses-feature
    android:name="android.hardware.vulkan.level"
    android:version="1"
    android:required="true" />

  <uses-feature
    android:name="android.hardware.vulkan.version"
    android:version="0x401000"
    android:required="true" />
		  
  <application
    android:icon="@mipmap/ic_launcher"
    android:label="{APP_NAME}"
	  android:debuggable="{APP_DEBUGGABLE}"
    android:testOnly="{APP_DEBUGGABLE}"
    android:hasCode="true"
    android:resizeableActivity="true"
	  android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
	  android:appCategory="{APP_CATEGORY}">
	  
    <activity android:name="net.osomi.nativeactivity.OxC3Activity"
	  			android:exported="{APP_DEBUGGABLE}"
            	android:configChanges="orientation|keyboard|keyboardHidden|screenLayout|fontScale|locale|touchscreen|density|uiMode|colorMode|layoutDirection|navigation">
			  
      <meta-data android:name="android.app.lib_name"
                 android:value="{APP_LIB_NAME}" />
				 
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
	  
    </activity>
  </application>

</manifest>