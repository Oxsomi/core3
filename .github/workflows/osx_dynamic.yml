name: macOS x64 Vulkan dynamic
on: [push]

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      name: macOS x64 Vulkan dynamic
      runner: macos-26-intel
      os: macos
      build_flags: '-dynamic_linking True -o cliGraphics=False'
      python_cmd: python
      setup_python: true
      python_version: '3.10'
      hook_name: macOS-x64-dynamic
    secrets:
      WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
